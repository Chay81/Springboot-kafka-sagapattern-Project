Important Note:
--------------

1. After creation of the customer, we need to use localhost:8083/login to generate token which is based on  Role_Authorization. This endpoint is only accessible
from customer-service cannot be used from Gateway. The role have been created inside the customer class itself with the below code.

@ElementCollection(fetch = FetchType.EAGER)
    @CollectionTable(name = "customer_roles", joinColumns = @JoinColumn(name = "customer_id"))
    @Column(name = "role")
    private Set<String> roles = new HashSet<>();

We can view the role_type in the DB table. SELECT * FROM kafka.customer_roles;

Admins have to be created using sql query below:

2. First create the customer 'Admin User' with the below query. If the password is like shown below, encrypt in online Bycrpt website: https://bcrypt-generator.com/ and paste the value in the password section, Like shown below.

INSERT INTO customer (customer_id, customer_name, email_address, password)
VALUES (2, 'Admin User', 'admin@example.com', '$2a$12$K2fBP030i33wcErxh0PPxeNa5n7lwIgMRre5.JTrBIo833mU8yTf6'); // password: admin@123

3. Once Admin is created use customer_id and provide a role like shown below. 

INSERT INTO customer_roles (customer_id, role)
VALUES (2, 'ROLE_ADMIN');

4. If using token with 'ROLE_CUSTOMER' then all the endpoints in this project will be available, except inventory-service which does not GET endpoint.
We need 'ROLE_ADMIN' token to update the inventory, using any other token we will get 403 error. And without using token we will get 401 error.

We can verify the token details and its details in the website: jwt.io. Also we can verify the duration of the token availability in this website


EndPoints:
----------

1. JsonEntries in Normal scenario
-------------------------------

POST Request to create an order
endpoint: localhost:8081/orders
if spring gateway is used
then the endpoint will be localhost:8765/orders

Note: when the order is placed, the stock gets deducted from inventory DB.

{
  "productName": "laptop",
  "quantity": 30,
  "price": 20000,
  "brandName":"Lenovo",
  "modelNumber":"14B"
}

Response in postman:

{
    "message": "Order placed and sent to Inventory Service!",
    "order": {
        "orderId": 35,
        "productName": "laptop",
        "quantity": 30,
        "price": 20000.0,
        "brandName": "Lenovo",
        "modelNumber": "14B",
        "status": "ORDER_PLACED"
    }
}

2. Get Request to fetch all order by productId
----------------------------------------------

Example: if the order id is 34 as shown above
then endpoint will be localhost:8081/orders/34
if spring gateway is used
then the endpoint will be localhost:8765/orders/34

Response:
--------

{
    "orderId": 34,
    "productName": "laptop",
    "quantity": 10,
    "price": 20000.0,
    "brandName": "Lenovo",
    "modelNumber": "14B",
    "status": "ORDER_PLACED"
}


3. 2. Get Request to fetch all order by productName
---------------------------------------------------

Example: if the productName is laptop as shown below
then endpoint will be localhost:8081/orders/product/laptop
if spring gateway is used
then the endpoint will be localhost:8765/orders/product/laptop

Response:
--------

[
    {
        "orderId": 34,
        "productName": "laptop",
        "quantity": 10,
        "price": 20000.0,
        "brandName": "Lenovo",
        "modelNumber": "14B",
        "status": "ORDER_PLACED"
    },
    
]


4. Post Request to create an order for Retry and DLQ scenario
-------------------------------------------------------------
Important Note: if running the endpoint without Spring Gateway, then the endpoint will be localhost:8081/orders, if
    using Spring Gateway, then the endpoint will be localhost:8765/orders

JsonEntries in Retry and DLQ scenario
{
  "productName": "fail-test",
  "price": 100.0,
  "brandName":"Lenovo",
  "modelNumber":"14Q",
  "quantity": 1,
  "status":"created"
}

if post request is made with the above JSON, it will fail and retry 2 times and then send the message to the dead letter topic (orders-dlt).
// The order will not be created in the inventory topic and the response will be as follows:
{
    "message": "Order failed to process in Inventory-Service, after 2 retries, sent to dead letter topic",
    "order": {
        "orderId": 8,
        "productName": "fail-test",
        "quantity": 1,
        "price": 100.0,
        "brandName": "Lenovo",
        "modelNumber": "14Q",
        "status": "Placed"
    }
}

Note: create below topics in Kafka before running the test
----------------------------------------------------------
inventory-topic
orders
orders-dlt

Make sure to run the Kafka server and create the topics using the commands from
kafka topics creation document.txt

Command to start/stop kafka server
----------------------------------

docker-compose up -d to start the server
docker-compose down to stop the server
